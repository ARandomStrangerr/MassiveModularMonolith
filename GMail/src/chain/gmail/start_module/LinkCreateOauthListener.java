package chain.gmail.start_module;

import chain.Chain;
import chain.Link;

public class LinkCreateOauthListener extends Link {
    public LinkCreateOauthListener(Chain chain) {
        super(chain);
    }

    @Override
    public boolean execute() {

        return false;
    }
}
